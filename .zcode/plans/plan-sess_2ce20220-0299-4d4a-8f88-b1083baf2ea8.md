# Plano final: Reformular criação de partida + tela de resultado (UX mobile otimizada)

Plano já aprovado pelo usuário. Executando em fases sequenciais (disparando subagentes por fase).

## Modelo de status
| Status DB | Rótulo | Botão admin |
|---|---|---|
| `draft` | Agendada | Finalizar |
| `published` | Resultado publicado + prazo votação | Editar |
| `closed` | Encerrada | Só leitura |

## Fases

### Fase 1 — ConfirmDialog.tsx (NOVO em src/components/)
- createPortal em document.body, backdrop fixed inset-0 z-50 bg-black/50 backdrop-blur-sm, card max-w-sm rounded-xl bg-white dark:bg-neutral-900.
- Props: open, onClose, onConfirm, titulo, mensagem?, textoConfirmar?, textoCancelar?, tomConfirmar? (destaque|perigo).
- ESC fecha, foco no confirmar, lock scroll, aria-modal/aria-labelledby, animação state-based. Botões min-h-[44px] active:scale-95.

### Fase 2 — PartidaNova.tsx
- Remover grid toggle + cards de time/gols + POSICAO_LABEL + Set selecionados.
- Lista de jogadores: linha por jogador (truncate min-w-0 flex-1) + 2 botões Preto/Branco (shrink-0).
- Contraste adaptativo por tema (não TIMES.cor fixo): Preto = bg-neutral-900 dark:bg-neutral-800 dark:border-neutral-400; Branco = bg-neutral-100 dark:bg-neutral-200 border-neutral-300.
- Comportamento re-clicar remove; neutro = opacity-60; bloqueio 8/time disabled.
- Pílulas Preto X/8 / Branco Y/8, verde + ✓ ao atingir 8.
- Feedback active:scale-95. podeSalvar = 8+8+data. pb-40, bottom-[calc(4rem+env(safe-area-inset-bottom))].

### Fase 3 — PartidaEditar.tsx (transformar placeholder em tela de resultado)
- Carrega partida+participantes, stats locais.
- 2 blocos de time com cabeçalho (TIMES.cor ok aqui) + contagem.
- Por participante: 3 steppers −valor+ (Gol ⚽ destaque, Assistência 🅰️ neutra, GC 🥅 vermelho + label "GC"). min-h-[44px] active:scale-95, clamp Math.max(0,...).
- Botão Salvar resultado (rodapé pb-40) → ConfirmDialog (publicar/aviso votos) → batch UPDATE partidas_participantes + (se draft) update status=published voting_closes_at=now+24h. closed = não editável.

### Fase 4 — PartidaDetalhe.tsx
- Rótulos: draft=Agendada, published=Resultado publicado (manter prazo), closed=Encerrada.
- draft&&isAdmin: link Finalizar → /partida/:id/editar. published&&isAdmin: link Editar.
- Remover publicar() e estado publicando.

### Fase 5 — Validar
- typecheck/lint/dev server.