# ═══════════════════════════════════════════════════════════════════════════
# PLAN METADATA
# ═══════════════════════════════════════════════════════════════════════════
plan_id: 20260724-futamigos-mvp
objective: "Construir MVP FutAmigos (APK Android): Expo app + Supabase backend (RLS + pg_cron + triggers) + WhatsApp share manual + notificacoes push, cobrindo fases 0-8 do PRD."
created_at: 2026-07-24T00:00:00-03:00
created_by: gem-planner
status: pending
tldr: |
  - MVP Android (APK) do app de pelada quinta-feira com gestao de presenca FIFO, financeiro unificado (mensalidade + avulso + caixa), sorteio aleatorio, sumula pos-jogo.
  - Stack: Expo + Supabase + NativeWind v4 + Zustand; auth Google (apenas); pg_cron dispara pushes BRT. Apple Sign-In pós-MVP.
  - 9 waves, 30 tasks, ~8-10 semanas part-time.

# ═══════════════════════════════════════════════════════════════════════════
# PLAN-LEVEL METRICS
# ═══════════════════════════════════════════════════════════════════════════
plan_metrics:
  wave_1_task_count: 7
  total_dependencies: 36
  risk_score: medium
  total_task_count: 26
quality_warnings:
  - "Tamagui vs NativeWind: plano fixa NativeWind v4 (dev solo, Expo Router parity). Revisar na Wave 1."
  - "Apple Sign-In no Android-only MVP questionavel (ver open_questions OB-1)."
  - "Edge Functions vs SQL puro para mensalidades/FIFO - plano assume SQL+pg_cron (OB-3)."
  - "Estrutura de diretorios (supabase/, app/) definida nesta plan, sujeita a ajustes."
  - "Timing do prompt de notificacao (OB-2): plano assume pos-login, validar em T1.4."
  - "Env split: .env.example (cliente/APK) so com EXPO_PUBLIC_*; .env.server.example (dev/migrations) com SUPABASE_SERVICE_ROLE_KEY + EXPO_ACCESS_TOKEN. SERVICE_ROLE NUNCA entra no APK (B-H1)."
  - "T2.0 MATCHES auto-creation depende pg_cron/pg_net habilitados em T1.3a (unblock Wave 2)."
  - "T5.0 Vault secret exige Supabase Vault habilitado no projeto (verificar dashboard)."
  - "T1.7 RLS foi movida para Wave 1 (antiga T5.3): RLS acompanha criacao de schema, nunca feature tardia (M1)."
resolved_decisions:
  - decision: "Dropar Apple Sign-In do MVP (somente Google OAuth)"
    rationale:
      [
        "PRD fixa APK Android apenas",
        "Apple Sign-In exige Apple Developer US$ 99/ano nativo iOS (protelado pós-MVP)",
        "MVP usa apenas Supabase + Google OAuth",
      ]
    affects: [T1.4, T8.2]
    resolved_at: 2026-07-24T00:00:00-03:00
open_questions:
  - question: "Apple Sign-In deve ser implementado no MVP se o escopo e APK Android apenas?"
    context: "PRD cita Apple como provedor OAuth, mas MVP e Android-only. Apple Sign-In exige Apple Developer $99/ano."
    type: decision_blocker
    affects: [T1.4, T8.2]
  - question: "Quando exibir prompt de notificacao (antes ou apos primeiro login)?"
    context: "Expo Notifications requer permissao explicita no Android 13+. Plano assume pos-login."
    type: decision_blocker
    affects: [T1.4]
  - question: "Usar Supabase Edge Functions para mensalidade/goleiro, ou SQL puro no pg_cron?"
    context: "PRD favorece pg_cron+SQL. Logica FIFO multi-condicional pode ser mais limpa em Edge Function."
    type: decision_blocker
    affects: [T3.1, T5.1]
  - question: "Onboarding do goleiro_pago: seed SQL ou UI admin sem login?"
    context: "Goleiros nao fazem login. Plano atual assume UI admin (T7.2) mas seed inicial em T1.3."
    type: decision_blocker
    affects: [T1.3, T7.2]
assumptions:
  - "Supabase project ja criado (entry criteria Wave 1). Supabase Vault habilitado (dashboard) - pre-req de T5.0."
  - "IDs OAuth Google ja obtidos (entry criteria Wave 1). Apple Sign-In dropado do MVP (resolved_decisions)."
  - "GitHub repo ja inicializado (entry criteria Wave 1)."
  - "Desenvolvimento em ambiente Windows com Node 20+ e Supabase CLI instalados."
  - "Fuso horario America/Sao_Paulo configurado no DB; timestamps em UTC."
  - "Mensalista = user_type='mensalista'; avulso = user_type='avulso' em PROFILES."
  - "1 pelada (group_id fixo) no MVP; seed GROUPS com UUID estavel."
  - "Env cliente (APK): somente EXPO_PUBLIC_* (URL, ANON_KEY, GOOGLE_WEB_CLIENT_ID). SERVICE_ROLE/EXPO_ACCESS_TOKEN ficam em .env.server.example, nunca no APK."
  - "Expo access token injetado no cron via current_setting('app.expo_token'); valor vem de Vault (T5.0)."

# ═══════════════════════════════════════════════════════════════════════════
# TASKS
# ══════════════════════════════════════════════════════════
tasks:
  # ═══════════════════════ WAVE 1: FASE 0 - SETUP INFRA ═══════════════════════
  - id: T1.1
    title: "Scaffold do app Expo + TS + Expo Router"
    description: "Inicializar projeto Expo SDK 52+ managed com TypeScript strict, Expo Router (file-based), estrutura de pastas (app/, components/, lib/, stores/, hooks/). Configurar tsconfig, eslint, prettier, .env scheme. Criar README com instruções de setup local."
    wave: 1
    agent: gem-implementer
    status: completed
    verified_at: 2026-07-24
    verified_by: gem-reviewer (verified_ok - strict/tsconfig/env split OK, sem ts-ignore/any/fictitious deps)
    covers: [fase-0-setup]
    dependencies: []
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "PRD - stack e diretorios"
    flags: { flaky: false, retries_used: 0, requires_design_validation: true }
    acceptance_criteria:
      - "app Expo inicializavel com `npx expo start` sem erros no Android/device."
      - "tsconfig.json strict=true, sem erros de tipo no template."
      - "Expo Router carrega rota /(tabs)/index sem crashing."
      - "Estrutura de pastas criada: app/, components/, lib/, stores/, hooks/, types/."
      - "README com comandos: start, lint, build:android."
    success_criteria:
      - "`npx expo start --android` abre app no emulador/dispositivo."
      - "`npm run lint` passa sem erros."
      - "`tsc --noEmit` passa."
    tech_stack:
      ["Expo SDK 52+", "React Native 0.76+", "TypeScript 5+", "Expo Router v3+"]
    test_coverage: "smoke manual em emulador"
    diag: null
    handoff:
      do_not_reinvestigate: ["stack-versao-exacta", "estrutura-pastas-twitter"]
      required_test_first: null
      target_files:
        ["app.json", "package.json", "tsconfig.json", "app/_layout.tsx"]
      minimal_change: "soh scaffold, sem telas de negocio"
      acceptance_checks: ["expo start funciona", "tsc strict OK"]

  - id: T1.2
    title: "Config Supabase CLI + estrutura migrations + env split"
    description: "Inicializar `supabase/` no repo, configurar supabase/config.toml referenciando o projeto remoto, criar diretorio `supabase/migrations/` versionado. Adicionar scripts npm (db:push, db:reset, db:seed). Validar `supabase db pull` contra projeto remoto. Criar .env.example (cliente/APK) e .env.server.example (dev/migrations)."
    wave: 1
    agent: gem-devops
    status: completed
    verified_at: 2026-07-24
    verified_by: gem-reviewer (verified_ok - env split cliente/server seguro, H1 OK, config.toml usa env() binding; microfix: GOOGLE_WEB_CLIENT_SECRET adicionado a supabase/.env.example)
    covers: [fase-0-setup, db-versioning]
    dependencies: []
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Schema DB e notes modelagem"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "`supabase init` rodou, pasta supabase/ existe."
      - "config.toml aponta para project-ref via env."
      - "Primeira migration vazia timestamped criada."
      - "package.json tem scripts db:push, db:reset local, db:seed."
      - ".env.example (cliente/APK) contem SOMENTE: EXPO_PUBLIC_SUPABASE_URL, EXPO_PUBLIC_SUPABASE_ANON_KEY, EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID."
      - ".env.server.example (dev/migrations) contem: SUPABASE_SERVICE_ROLE_KEY, EXPO_ACCESS_TOKEN. Documentacao inline: SERVICE_ROLE NUNCA entra no APK."
    success_criteria:
      - "`supabase db pull` gera schema inicial sem erro."
      - "`npm run db:push` aplica migrations no projeto remoto."
    environment: development
    requires_approval: true
    devops_security_sensitive: true
    handoff:
      do_not_reinvestigate: ["project-ref-obtido"]
      required_test_first: null
      target_files:
        [
          "supabase/config.toml",
          "supabase/migrations/00000000000000_init.sql",
          ".env.example",
          ".env.server.example",
        ]
      minimal_change: "soh setup CLI + env split"
      acceptance_checks:
        ["db pull OK", "migrations versionadas", "env split cliente/server"]

  - id: T1.3a
    title: "Schema SQL DDL: tabelas, enums, FKs, indexes"
    description: "Migration com TODA a DDL: GROUPS, PROFILES, MATCHES, MATCH_PRESENCES, MATCH_PARTICIPANTS, PAYMENTS, EXPENSES, DEVICE_TOKENS. Incluir FKs (cascade rules explicitas), indexes (match_id, user_id, group_id), check constraints. Criar enums: user_type, match_status, rsvp_status, payment_type, payment_status, expense_type. Habilitar extensoes: pg_cron (schema cron), pg_net, e verificar Supabase Vault habilitado (pre-req de T5.0). Aderencia estrita ao ERD do PRD. SEM seed ainda (T1.3b) e SEM RLS ainda (T1.7)."
    wave: 1
    agent: gem-implementer
    status: completed
    verified_at: 2026-07-24
    verified_by: gem-reviewer (verified_ok apos fixes; paridade PRD, 8 tabelas + 6 enums + 15 indexes OK; fk_auth_users confirmation: manter FK, seed T1.3b via auth.admin.createUser Node script)
    covers: [fase-0-schema]
    dependencies: [T1.2]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Secao arquitetura banco + ERD"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "Migration aplica sem erro em DB resetado."
      - "Todas as 8 tabelas presentes com colunas exatas do PRD."
      - "FKs e constraints validas (cascade rules explicitas)."
      - "Enums criados: user_type, match_status, rsvp_status, payment_type, payment_status, expense_type."
      - "Indexes criados para FKs comuns (match_id, user_id, group_id)."
      - "Extensao pg_cron habilitada; verificacao de pg_net e vault documentada."
    success_criteria:
      - "`supabase db reset` roda migration sem erro."
      - "Query em `information_schema.tables` confirma 8 tabelas + enums."
    tech_stack: ["PostgreSQL 15", "Supabase", "SQL DDL"]
    test_coverage: "queries SQL validando schema"
    diag: null
    handoff:
      do_not_reinvestigate: ["schema-erd-prd"]
      required_test_first: null
      target_files: ["supabase/migrations/00000000000001_schema.sql"]
      minimal_change: "soh DDL tabelas/enums/indexes, sem seed, sem RLS"
      acceptance_checks: ["8 tabelas presentes", "enums OK", "FKs validas"]

  - id: T1.3b
    title: "Seed SQL: GROUPS + goleiros + admin fake"
    description: "Migration de seed: (a) 1 GROUPS estavel com UUID hardcoded (name='Racha Quintas', day_of_week=4, monthly_fee, default_casual_fee=R$20, goalkeeper_expense=R$40). (b) 2 PROFILES goleiro_pago sem auth.users (user_type='goleiro_pago'). (c) >=1 PROFILES admin fake (is_admin=true, user_type='mensalista') sem auth.users - permite ao primeiro usuario real ser promovido manualmente via UPDATE no console Supabase (ver handoff comment). Constraints adicionais se necessario. Nao depende de OAuth para existir."
    wave: 1
    agent: gem-implementer
    status: completed
    verified_at: 2026-07-24
    verified_by: gem-reviewer (verified_ok apos fix dupla tsx em devDeps; seed.sql so groups, seed-auth.ts idempotente, sem leak service_role, async/await, sem any)
    covers: [fase-0-schema, fase-1-seed]
    dependencies: [T1.3a]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 3 goleiros, regra 6 admin"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "Seed cria GROUPS com UUID estavel e defaults do PRD (R$20, R$40)."
      - "Seed cria 2 PROFILES goleiro_pago sem auth.users."
      - "Seed cria >=1 PROFILES admin fake (is_admin=true, user_type='mensalista') sem auth.users."
      - "`supabase db reset` aplica seed sem erro."
      - "Handoff comment SQL explicita como promover primeiro usuario real."
    success_criteria:
      - "Query confirma 1 GROUPS + 2 goleiros + 1 admin fake."
      - "FKs validas apos seed (group_id referenciado)."
    tech_stack: ["PostgreSQL 15", "Supabase"]
    test_coverage: "queries SQL validando seed"
    diag: null
    handoff:
      do_not_reinvestigate: ["group-uuid-estavel"]
      required_test_first: null
      target_files:
        ["supabase/seed.sql", "supabase/migrations/00000000000002_seed.sql"]
      minimal_change: "soh seed, sem RLS, sem mais schema"
      acceptance_checks: ["group seed OK", "2 goleiros", "1 admin fake"]
      comment_to_dev: "Apos primeiro login OAuth real, promover manualmente via Supabase Studio: UPDATE PROFILES SET is_admin=true WHERE id = '<auth.users.id>';"

  - id: T1.7
    title: "RLS policies em todas as 8 tabelas (Wave 1 - acompanha schema)"
    description: "Implementar RLS junto com o schema (nunca feature tardia - M1). Row Level Security em todas as 8 tabelas conforme PRD regra 6 + transparencia. Policies: PROFILES (usuario ve proprio + todos do seu group_id; admin ve tudo do group). MATCHES (todos do group veem). MATCH_PRESENCES (usuario insere proprio; admin update todos). PAYMENTS (todos do group veem todos pagamentos; jogador marca soh seus; admin aprova todos). EXPENSES (soh admin mutate; todos do group leem). DEVICE_TOKENS (soh proprio). MATCH_PARTICIPANTS (todos do group leem; admin mutate). Helpers SQL is_admin() e is_group_member(group_id)."
    wave: 1
    agent: gem-implementer
    status: pending
    covers: [fase-0-rls]
    dependencies: [T1.3a, T1.3b]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 6 permissoes + transparencia"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "RLS ENABLE em todas as 8 tabelas."
      - "Policy por tabela cobre SELECT, INSERT, UPDATE, DELETE aplicavel."
      - "Helpers SQL is_admin() e is_group_member(group_id) usados nas policies."
      - "Teste com 2 usuarios (comum + admin) valida isolamento."
      - "Goleiro_pago (sem auth) acessivel apenas via service_role."
    success_criteria:
      - "2 contas teste: comum ve soh dados do seu group, admin muta tudo."
      - "Cross-group SELECT retorna vazio (isolamento por group_id)."
    tech_stack: ["PostgreSQL RLS", "Supabase Auth"]
    test_coverage: "script SQL com 2 roles de teste"
    diag: null
    handoff:
      do_not_reinvestigate: ["rls-policy-list"]
      required_test_first: null
      target_files: ["supabase/migrations/00000000000003_rls.sql"]
      minimal_change: "soh policies, sem alterar schema"
      acceptance_checks:
        ["8 tabelas c/ RLS", "isolamento OK", "is_admin() funciona"]

  - id: T1.4
    title: "Config OAuth Google + Supabase client + persistencia push token"
    description: "Configurar Supabase JS client (`lib/supabase.ts`) com env vars do .env.example (EXPO_PUBLIC_*), provider Google OAuth (Expo Auth Session + Google Sign-In ou web flow), criar hook `useAuth()`. Criar tela de login PT-BR apenas com botao 'Entrar com Google' (Apple Sign-In dropado do MVP - vide resolved_decisions). Exibir prompt de notificacao logo apos login bem-sucedido (Android 13+). Apos permissao concedida, executar getExpoPushTokenAsync() e INSERT INTO DEVICE_TOKENS (user_id, expo_push_token) - sem isso T5.2 dispatch falha (blocker B6)."
    wave: 1
    agent: gem-implementer
    status: pending
    covers: [fase-0-oauth, fase-1-auth, fase-1-devicetoken]
    dependencies: [T1.1, T1.3a, T1.5]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Secao Auth (regra 2) + regra 5 device tokens"
    flags: { flaky: false, retries_used: 0, requires_design_validation: true }
    acceptance_criteria:
      - "lib/supabase.ts exporta client(config) singleton lendo EXPO_PUBLIC_*."
      - "`useAuth()` retorna { user, signInWithGoogle, signOut, loading }."
      - "Login Google funcional em device Android (retorna session Supabase)."
      - "Tela de login PT-BR com copy e botoes conforme design system T1.5."
      - "Apenas botao Google (Apple Sign-In removido do MVP)."
      - "Prompt de notificacao Expo exibido apos login (Android 13+)."
      - "Apos permissao: getExpoPushTokenAsync() retorna token e INSERT em DEVICE_TOKENS (user_id, expo_push_token) com ON CONFLICT para evitar duplicatas."
    success_criteria:
      - "Login Google cria sessao valida e PROFILES populado via trigger T1.5."
      - "Botao voltar de logout funciona."
      - "Token Expo visivel em DEVICE_TOKENS apos primeiro login com permissao."
    tech_stack:
      [
        "@supabase/supabase-js",
        "expo-auth-session",
        "expo-web-browser",
        "@react-native-google-signin/google-signin",
        "expo-notifications",
      ]
    test_coverage: "fluxo manual login+logout+token no device"
    diag: null
    handoff:
      do_not_reinvestigate: ["client-id-google"]
      required_test_first: null
      target_files:
        [
          "lib/supabase.ts",
          "app/login.tsx",
          "hooks/useAuth.ts",
          "lib/pushToken.ts",
        ]
      minimal_change: "auth flow + 1 tela de login + persistencia token"
      acceptance_checks:
        [
          "Google sign-in funcional",
          "prompt notif aparece",
          "token em DEVICE_TOKENS",
        ]

  - id: T1.5
    title: "Design system + Tema + trigger auth.users->PROFILES (defaults explicitos)"
    description: "Duas frentes paralelas: (a) Configurar design system base (cores, tipografia, espacamentos) com NativeWind v4 + Tailwind config PT-BR friendly (nao usa framework i18n no MVP). (b) Migration SQL com trigger `on insert auth.users` que insere em PROFILES (`on conflict do nothing`), pegando full_name/avatar_url dos metadados OAuth. Defaults EXPLICITOS obrigatorio (M2): user_type='avulso', group_id=<UUID seed GROUPS em T1.3b>, is_admin=false, avatar_url=NULL. Esta task e' gate para auth T1.4."
    wave: 1
    agent: gem-designer
    status: pending
    covers: [fase-0-trigger, fase-1-theme]
    dependencies: [T1.3b]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Stack, tema, regra 2 trigger auth"
    flags: { flaky: false, retries_used: 0, requires_design_validation: true }
    acceptance_criteria:
      - "Migration com trigger FUNCTION handle_new_user() + TRIGGER on_auth_user_created."
      - "Trigger: insere PROFILES apenas se nao existe (idempotente)."
      - "Defaults EXPLICITOS no INSERT: user_type='avulso', group_id=<UUID seed GROUPS>, is_admin=false, avatar_url=NULL."
      - "native-wind.config + tailwind.config definem palette (primaria verde campo, neutros), typography scale, spacing."
      - "Componente Button reutilizavel PT-BR exemplificado."
      - "App consome classes NativeWind em ao menos 1 tela de teste."
    success_criteria:
      - "Novo OAuth login cria PROFILES automaticamente com defaults corretos (user_type='avulso', group correto)."
      - "Classes Tailwind aplicadas em runtime."
    tech_stack: ["NativeWind v4", "Tailwind CSS", "PostgreSQL triggers"]
    test_coverage: "trigger testado via insert simulado em auth.users"
    diag: null
    handoff:
      do_not_reinvestigate: ["palette-token-final", "group-uuid-default"]
      required_test_first: null
      target_files:
        [
          "supabase/migrations/00000000000004_auth_trigger.sql",
          "tailwind.config.js",
          "global.css",
        ]
      minimal_change: "trigger + theme base"
      acceptance_checks:
        [
          "PROFILES auto-criado em novo login",
          "defaults explicitos corretos",
          "NativeWind aplicado",
        ]

  # ═══════════════════════ WAVE 2: FASE 1-2 CORE APP ═══════════════════════
  - id: T2.0
    title: "MATCHES auto-criacao: seed proxima quinta + job pg_cron semanal + UI admin manual"
    description: "Resolver blocker B5: garantir que sempre exista um MATCHES futuro valido para FK de MATCH_PRESENCES. Tres componentes: (a) Seed inicial na primeira execucao: inserir proxima quinta-feira 19:00 America/Sao_Paulo (calculo em SQL via generate_series/date_trunc). (b) Job pg_cron semanal toda Sexta-feira 20:00 BRT (Sexta 23:00 UTC) que cria automaticamente o MATCHES da quinta-feira seguinte (idempotente via unique(group_id, date_time)). (c) UI admin para criar/editar/cancelar match manualmente (substituir campo, status=cancelled) - flexibilidade operacional. Esta task e' gate de FK para T2.2 ( RSVP UI) e T2.3 (desistencia)."
    wave: 2
    agent: gem-implementer
    status: pending
    covers: [fase-0-cron, fase-2-matches]
    dependencies: [T1.3a, T1.3b, T1.7]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 4 partidas quinta, regra 5 cronograma"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "Seed cria MATCHES para proxima quinta 19:00 BRT baseado em now() + calculo de dia da semana."
      - "Job pg_cron 'create_next_weekly_match' registrado: toda Sexta 20:00 BRT (Sexta 23:00 UTC)."
      - "Job e' idempotente (ON CONFLICT (group_id, date_time) DO NOTHING via unique constraint)."
      - "UI admin: tela de criar/editar match com date_time picker, botao Cancelar (seta status=cancelled)."
      - "MATCHES.status apos criacao = 'scheduled'."
      - "Logs em cron.job_run_details."
    success_criteria:
      - "Apos db reset: existe MATCHES para a proxima quinta."
      - "Disparar job manualmente cria match da semana seguinte sem duplicar."
      - "Admin consegue cancelar um match e criar substituto via UI."
      - "T2.2/T2.3 tem FK match_id valida para operar."
    tech_stack: ["PostgreSQL", "pg_cron", "Supabase RPC", "React Native"]
    test_coverage: "teste manual: disparar job + validar seed"
    diag: null
    handoff:
      do_not_reinvestigate: ["next-thursday-calc"]
      required_test_first: null
      target_files:
        [
          "supabase/migrations/00000000000005_matches.sql",
          "app/(tabs)/admin/matches.tsx",
        ]
      minimal_change: "seed + job semanal + UI admin only"
      acceptance_checks:
        [
          "seed proxima quinta existe",
          "job semanal cria match",
          "UI admin funcional",
        ]

  - id: T2.1
    title: "Layout principal + tabs + Zustand stores"
    description: "Implementar Expo Router layout `(tabs)` com 4 abas: Presenca, Caixa, Sorteio, Perfil. Criar stores Zustand (`usePresenceStore`, `useMatchStore`, `usePaymentStore`, `useProfileStore`) com selectors tipados. Configurar Realtime subscriptions para MATCH_PRESENCES e PAYMENTS. helpers de timezone (BRT<->UTC)."
    wave: 2
    agent: gem-implementer
    status: pending
    covers: [fase-1-core, fase-2-shell]
    dependencies: [T1.4, T1.5]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Navegacao e stores"
    flags: { flaky: false, retries_used: 0, requires_design_validation: true }
    acceptance_criteria:
      - "Layout tabs carrega sem erro em device real."
      - "4 abas presentes e roteaveis."
      - "Stores Zustand expostos via hooks com tipagem forte."
      - "Helpers `toBRT(iso)` e `toUTC(brt)` com testes rapidos."
      - "Realtime sub em MATCH_PRESENCES reflete updates sem reload."
    success_criteria:
      - "Navegacao fluida entre 4 abas."
      - "Mutation em DB reflete na UI via realtime."
    tech_stack: ["Expo Router", "Zustand", "@supabase/supabase-js Realtime"]
    test_coverage: "teste manual realtime"
    diag: null
    handoff:
      do_not_reinvestigate: ["tab-list-final"]
      required_test_first: null
      target_files: ["app/(tabs)/_layout.tsx", "stores/*.ts", "lib/timezone.ts"]
      minimal_change: "shell + stores vazios"
      acceptance_checks: ["4 tabs carregam", "realtime update OK"]

  - id: T2.2
    title: "Tela presenca: listas confirmados/fila/espera"
    description: "Tela principal 'Presenca': cards visuais separando [Confirmados / Pendentes aprovacao / Fila espera FIFO]. Aderencia ao fluxo do PRD: mensalista auto-confirma, avulso fica pending_approval. Exibir corte visual (linha) na capacidade 16. Usar NativeWind para styling, PT-BR copy."
    wave: 2
    agent: gem-designer
    status: pending
    covers: [fase-2-lista]
    dependencies: [T2.1]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 4 RSVP, capacidades"
    flags: { flaky: false, retries_used: 0, requires_design_validation: true }
    acceptance_criteria:
      - "3 listas (confirmados, pendentes, fila) claramente separadas visualmente."
      - "Capacidade da lista de confirmados exibida (X/16)."
      - "Cards de jogador com nome, user_type badge. (Campo morto 'avatar' removido - N2.)"
      - "Cores de estado: confirmado=verde, pendente=amarelo, desistente=cinza."
      - "Copy 100% PT-BR."
    success_criteria:
      - "Designer valida hierarquia visual."
      - "Implementer consegue consumir layout para bind de dados T2.3."
    tech_stack: ["React Native", "NativeWind v4", "FlashList (opcional)"]
    test_coverage: null
    diag: null
    handoff:
      do_not_reinvestigate: ["layout-cards-jogador"]
      required_test_first: null
      target_files:
        [
          "app/(tabs)/index.tsx",
          "components/PlayerCard.tsx",
          "components/PresenceList.tsx",
        ]
      minimal_change: "UI estatica com mock data"
      acceptance_checks: ["3 listas renderizam", "PT-BR copy OK"]

  - id: T2.3
    title: "Logica RSVP + cambio de status + cutoff rebate fila FIFO"
    description: "Implementar logica de RSVP conforme PRD regra 4: (a) Mensalista confirma -> status=confirmed direto. (b) Avulso confirma -> status=pending_approval. (c) Botao Desistir em confirmado. (d) Validar cutoff terca 19h BRT. (e) Apos cutoff terca 19h BRT: mensalista desistente que re-confirma presenca e' tratado como novo avulso - INSERT MATCH_PRESENCES (status='waiting_list') no final da fila FIFO (M4 rebate). Expor RN calls a partir do PresenceStore."
    wave: 2
    agent: gem-implementer
    status: pending
    covers: [fase-2-rsvp]
    dependencies: [T2.0, T2.1, T2.2]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 4 presenca e cutoff"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "Mensalista botao Confirmar -> DB status=confirmed."
      - "Avulso botao Confirmar -> DB status=pending_approval."
      - "Cutoff terca 19h BRT bloqueia re-confirmacao mensalista desistente como mensalista."
      - "Apos cutoff: re-confirmacao mensalista desistente gera novo INSERT status='waiting_list' (final da fila FIFO) - rebate M4."
      - "Validacao: avulso nao pode pular fila."
      - "Erros exibidos via toast PT-BR."
    success_criteria:
      - "4 cenarios de RSVP testados manualmente em device."
      - "Cutoff rebate FIFO respeitado via helper timezone T2.1."
      - "FK match_id valida (garantida por T2.0 gate)."
    tech_stack: ["Zustand", "Supabase JS", "date-fns-tz"]
    test_coverage: "teste manual de 5 cenarios (inclui rebate FIFO)"
    diag: null
    handoff:
      do_not_reinvestigate: ["cutoff-brt-logica", "fifo-rebate-rule"]
      required_test_first: null
      target_files: ["stores/presence.ts", "lib/rsvp.ts"]
      minimal_change: "logica RSVP only, sem UI"
      acceptance_checks:
        ["5 cenarios RSVP OK", "cutoff bloqueia", "rebate FIFO OK"]

  # ═══════════════════════ WAVE 3: FASE 2 AVANÇO + WHATSAPP ═══════════════════════
  - id: T3.1
    title: "Promocao FIFO avulso + UI admin"
    description: "Implementar promocao FIFO automatica e manual: quando mensalista desiste (status=declined) e ha avulso na fila, o primeiro avulso pendente e' promovido a status=confirmed. Cenario recursivo: se 1o avulso recusar, tenta proximo. UI admin: lista de pendentes com bot\u00f5es Promover/Rejeitar. Logica no DB via SQL function `promote_next_casual(group_id, match_id)` chamada pelo client admin."
    wave: 3
    agent: gem-implementer
    status: pending
    covers: [fase-2-fifo]
    dependencies: [T2.3]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 4 promocao FIFO"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "Function SQL promote_next_casual existe e e' transactional."
      - "Mensalista desistente com avulso em fila -> proximo promovido."
      - "Avulso rejeitado -> tenta proximo ate esgotar ou preencher."
      - "Sem avulso -> vaga fica aberta sem erro."
      - "UI admin lista pendentes com Promover/Rejeitar (PT-BR)."
    success_criteria:
      - "Cenario FIFO de 3 avulsos testado end-to-end."
      - "Performance: function usa OFFSET/LIMIT sem scan completo."
    tech_stack: ["PostgreSQL functions", "Supabase RPC", "React Native"]
    test_coverage: "teste manual cenario FIFO 3 jogadores"
    diag: null
    handoff:
      do_not_reinvestigate: ["fifo-sql-algoritmo"]
      required_test_first: null
      target_files:
        [
          "supabase/migrations/00000000000003_fifo.sql",
          "app/(tabs)/admin/pending.tsx",
        ]
      minimal_change: "function + UI admin only"
      acceptance_checks: ["3 cenarios FIFO OK", "vaga aberta sem erro"]

  - id: T3.2
    title: "Gerador de mensagens WhatsApp via Web Share + fallback clipboard"
    description: "Implementar gerador de mensagens PT-BR formatadas para 3 categorias: (a) 'Texto de Segunda': lista completa de confirmados/pendentes/fila para o grupo do racha. (b) 'Alerta 48h' (Ter 19h): recap lista final. (c) 'Novidade de confirmacao': avulso promovido. Usar expo-sharing ou Linking Deep Link `whatsapp://send?text=`. Validar abertura no WhatsApp nativo. Fallback (L3): se WhatsApp nao instalado, copiar texto formatado para clipboard (expo-clipboard) + exibir toast PT-BR 'Texto copiado! Cole no WhatsApp.'"
    wave: 3
    agent: gem-implementer
    status: pending
    covers: [fase-3-whatsapp]
    dependencies: [T3.1]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 5 WhatsApp manual"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "3 templates PT-BR renderizam texto formatado correto."
      - "Deep Link `whatsapp://send?text=...` abre WhatsApp nativo quando instalado."
      - "Fallback Web Share API (expo-sharing) se WhatsApp nao instalado."
      - "Fallback final (L3): se nenhum share disponivel, copiar texto para clipboard + toast 'Texto copiado! Cole no WhatsApp.' (PT-BR)."
      - "Botao 'Compartilhar' em tela de presenca acessivel a admin."
      - "Texto encode UTF-8 correto (acentos)."
      - "Sem referencia ao campo morto 'avatar' (N2)."
    success_criteria:
      - "Admin compartilha lista de confirmados para grupo WhatsApp real."
      - "Texto chega bem formatado no WhatsApp."
      - "Sem WhatsApp instalado -> clipboard + toast funcionam."
    tech_stack:
      ["expo-sharing", "expo-linking", "expo-clipboard", "React Native"]
    test_coverage: "teste manual em device real (com e sem WhatsApp)"
    diag: null
    handoff:
      do_not_reinvestigate: ["whatsapp-deeplink-format", "clipboard-fallback"]
      required_test_first: null
      target_files: ["lib/whatsapp.ts", "components/ShareButton.tsx"]
      minimal_change: "somente geradores de mensagem + fallback"
      acceptance_checks:
        ["3 templates OK", "whatsapp abre", "clipboard fallback OK"]

  # ═══════════════════════ WAVE 4: FASE 4 FINANCEIRO ═══════════════════════
  - id: T4.1
    title: "Painel financeiro unificado (PAYMENTS)"
    description: "Tela 'Caixa' mostrando lista de PAYMENTS (mensalidades + avulsos) com columnas: jogador, tipo (Mensal/Avulso), valor, status (pending/marked/paid). Agrupar por mes corrente. Painel transparente conforme regra 6: todos usuarios veem todos pagamentos do grupo. filtros por status."
    wave: 4
    agent: gem-designer
    status: pending
    covers: [fase-4-financeiro-ui]
    dependencies: [T1.3a, T1.7, T2.1]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 3 financeiro, regra 6 transparencia"
    flags: { flaky: false, retries_used: 0, requires_design_validation: true }
    acceptance_criteria:
      - "Lista agrupada por mes com totais por status."
      - "Filtros: Todos / Pendentes / Marcados / Pagos."
      - "Copy PT-BR (R$ formatado em pt-BR)."
      - "Sem valores privados - todos do GROUPS.default."
      - "Usuario comum e admin veem mesma lista (transparencia garantida por RLS T1.7)."
    success_criteria:
      - "Design aprovado antes do bind de dados."
    tech_stack: ["React Native", "NativeWind v4", "Intl PT-BR"]
    test_coverage: null
    diag: null
    handoff:
      do_not_reinvestigate: ["layout-caixa"]
      required_test_first: null
      target_files: ["app/(tabs)/caixa.tsx", "components/PaymentRow.tsx"]
      minimal_change: "UI estatica com mock"
      acceptance_checks: ["grupos por mes OK", "PT-BR valores"]

  - id: T4.2
    title: "Fluxo dupla confirmacao pagamento"
    description: "Implementar fluxo dupla confirmacao PRD regra 3: (a) Jogador marca como pago -> PAYMENTS.marked_paid_at. (b) Admin aprova -> PAYMENTS.approved_at + paid_at. UI jogador: botao 'Marquei como pago'. UI admin: lista de marcados com 'Aprovar'. Validar RLS: soh proprio jogador marca, soh admin aprova."
    wave: 4
    agent: gem-implementer
    status: pending
    covers: [fase-4-pagamento]
    dependencies: [T1.7, T4.1]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "Jogador marca pagamento proprio -> marked_paid_at set."
      - "Admin ve lista de marcados, aprova -> approved_at + paid_at."
      - "RLS impede jogador marcar pagamento de outro."
      - "RLS impede nao-admin aprovar."
      - "Realtime reflete aprovacao instantanea na lista."
    success_criteria:
      - "Cenario dupla confirmacao testado com 2 users."
      - "RLS testado com 2 contas diferentes."
    tech_stack: ["Supabase RLS", "Zustand", "React Native"]
    test_coverage: "teste manual com 2 contas"
    diag: null
    handoff:
      do_not_reinvestigate: ["rls-payments"]
      required_test_first: null
      target_files: ["lib/payments.ts", "app/(tabs)/admin/payments.tsx"]
      minimal_change: "flows de pagamento only"
      acceptance_checks: ["dupla confirm OK", "RLS bloqueia cross-user"]

  - id: T4.3
    title: "Tabela EXPENSES + UI + reminder goleiros"
    description: "Implementar gestao de EXPENSES: (a) UI admin para criar despesa (tipo goalkeeper/field/other, descricao, valor). (b) Lista de expenses com toggle confirmed_at. (c) Push reminder pos-jogo para admin: 'Confirme pagamento dos goleiros de R$ 40'. Default value de GROUPS.goalkeeper_expense."
    wave: 4
    agent: gem-implementer
    status: pending
    covers: [fase-4-expenses, fase-5-caixa]
    dependencies: [T4.2]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 3 goleiros + caixa"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "Admin cria EXPENSES tipo goalkeeper valor default do GROUPS.goalkeeper_expense (R$40)."
      - "Lista de expenses com toggle confirmed_at."
      - "Push reminder dispara quando MATCHES.status -> finished."
      - "Saldo do caixa = SUM(approved payments) - SUM(confirmed expenses) calculado em tempo real."
    success_criteria:
      - "Cenario: jogo finalizado -> admin recebe push -> cria expense -> confirma."
    tech_stack: ["Supabase JS", "Expo Notifications", "React Native"]
    test_coverage: "teste manual cenario pos-jogo"
    diag: null
    handoff:
      do_not_reinvestigate: ["expense-default-formula"]
      required_test_first: null
      target_files: ["lib/expenses.ts", "app/(tabs)/admin/expenses.tsx"]
      minimal_change: "CRUD expenses + reminder"
      acceptance_checks: ["CRUD OK", "saldo calc OK"]

  # ═══════════════════════ WAVE 5: FASE 4 CRON + FASE 5 CAIXA ═══════════════════════
  - id: T5.0
    title: "Supabase Vault secret: Expo access token para pg_cron dispatch"
    description: "Resolver blocker H3: armazenar EXPO_ACCESS_TOKEN de forma segura no Supabase Vault (NUNCA em .env do cliente/APK, NUNCA hardcoded em migration). Criar secret via vault.create_secret('expo_access_token', '<token>'). Documentar query SQL via vault.decrypted_secret(...). Comandos cron.schedule devem injetar o token via current_setting('app.expo_token') setado dinamicamente no job. Pre-req para T5.2 dispatch de push."
    wave: 5
    agent: gem-devops
    status: pending
    covers: [fase-0-vault, fase-5-secrets]
    dependencies: [T1.3a]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 5 push via pg_cron + pg_net"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "Supabase Vault habilitado (verificar em dashboard/extensions)."
      - "Secret 'expo_access_token' criado via vault.create_secret(...)."
      - "Query de teste `SELECT vault.decrypted_secret('expo_access_token')` retorna o token."
      - "Documentacao em .env.server.example: EXPO_ACCESS_TOKEN (somente dev/migrations), nunca no APK."
      - "Padrao documentado para cron.schedule usar `SET LOCAL app.expo_token = (SELECT vault.decrypted_secret('expo_access_token'))` dentro do job."
    success_criteria:
      - "secret vault existente e recuperavel."
      - "Nenhum token Expo hardcoded em arquivos versionados."
    environment: development
    requires_approval: true
    devops_security_sensitive: true
    handoff:
      do_not_reinvestigate: ["vault-enabled"]
      required_test_first: null
      target_files:
        [
          "supabase/migrations/00000000000006_vault.sql",
          ".env.server.example",
          "docs/plan/20260724-futamigos-mvp/secrets-vault.md",
        ]
      minimal_change: "secret vault only + docs"
      acceptance_checks: ["vault secret existe", "decrypted_secret funciona"]

  - id: T5.1
    title: "pg_cron job dia 5 mensalidades"
    description: "Criar job pg_cron (extensao ja habilitada em T1.3a) que roda todo dia 5 09:00 BRT (12:00 UTC) e gera PAYMENTS tipo monthly para todos os mensalistas ativos (user_type='mensalista') do group. Job SQL idempotente evitando duplicacao no mesmo mes. Job reference: cron.schedule numa migration."
    wave: 5
    agent: gem-devops
    status: pending
    covers: [fase-4-dia5, fase-0-cron]
    dependencies: [T1.3a, T1.7, T4.2]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 5 dia 5 mensalidades"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "Job `cron.job` registrado todo dia 5 09:00 BRT (12:00 UTC)."
      - "Job gera PAYMENTS para todos mensalistas (seed com mensalistas)."
      - "Job e' idempotente (nao duplica se rodar 2x no mesmo mes)."
      - "Logs em cron.job_run_details visiveis."
    success_criteria:
      - "Teste manual: `SELECT cron.schedule(...)` + disparar manualmente valida criacao."
      - "Mensalidades criadas na execucao do job (1 por mensalista)."
    environment: development
    requires_approval: true
    devops_security_sensitive: false
    handoff:
      do_not_reinvestigate: ["pg_cron-enabled-ver-T1.3a"]
      required_test_first: null
      target_files: ["supabase/migrations/00000000000007_cron_dia5.sql"]
      minimal_change: "soh job dia 5"
      acceptance_checks: ["job registrado", "mensalidades geradas sem duplicar"]

  - id: T5.2
    title: "Jobs pg_cron semanal (Seg/Ter/Qui) + push dispatch via Vault"
    description: "Criar 3 jobs pg_cron semanais + logica de dispatch push: (1) Seg 09:00 BRT: lembrete mensalistas confirmarem. (2) Ter 19:00 BRT (48h antes): recap lista. (3) Qui 18:00 BRT: lembrete do jogo. Job chama SQL function que busca DEVICE_TOKENS ativos (populados em T1.4) e enfileira pushes via pg_net POST para Expo Push API. Auth bearer localizado no Vault via T5.0; injetado no setting app.expo_token."
    wave: 5
    agent: gem-devops
    status: pending
    covers: [fase-0-cron, fase-1-push]
    dependencies: [T5.0, T5.1, T1.4]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 5 cronograma push"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "3 jobs cron.schedule criados (Seg/Ter/Qui nos horarios BRT convertidos para UTC)."
      - "Function SQL get_active_push_tokens(group_id) retorna tokens de DEVICE_TOKENS."
      - "Dispatch via pg_net POST para Expo Push API com auth bearer do Vault (T5.0)."
      - "Token injetado via `SET LOCAL app.expo_token = (SELECT vault.decrypted_secret('expo_access_token'))`."
      - "Logs de push enviados em tabela push_log (audit)."
      - "Erros de entrega (429/400) logados."
    success_criteria:
      - "Teste manual disparando job retorna push real no device com token persistido em DEVICE_TOKENS."
      - "Limite 600/s respeitado (batch de tokens)."
    environment: development
    requires_approval: true
    devops_security_sensitive: true
    handoff:
      do_not_reinvestigate: ["expo-push-endpoint", "vault-token-injection"]
      required_test_first: null
      target_files: ["supabase/migrations/00000000000008_push_jobs.sql"]
      minimal_change: "3 jobs + dispatch via Vault"
      acceptance_checks: ["3 jobs registrados", "push chega no device"]

  - id: T5.4
    title: "EAS Build config + primeiro build APK"
    description: "Configurar EAS Build (eas.json) com profile `preview` (APK internal), `production` (AAB loja). Definir android.package, versionCode, versionName. Definir env vars injetadas no build SOMENTE do .env.example cliente (EXPO_PUBLIC_*); SERVICE_ROLE NUNCA no APK (B-H1). Rodar `eas build -p android --profile preview` e validar APK instalando em dispositivo real."
    wave: 5
    agent: gem-devops
    status: pending
    covers: [fase-0-eas]
    dependencies: [T1.1, T1.4]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Stack + EAS"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "eas.json valido com 2 profiles (preview/production)."
      - "Package ID definido (ex: com.futamigos.app)."
      - "Build APK concluido sem erro."
      - "APK instala e abre em Android real."
      - "Env vars injetadas no APK: EXPO_PUBLIC_SUPABASE_URL, EXPO_PUBLIC_SUPABASE_ANON_KEY, EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID somente."
      - "APK NAO contem SUPABASE_SERVICE_ROLE_KEY nem EXPO_ACCESS_TOKEN (validar via grep no bundle)."
    success_criteria:
      - "APK funcional em device real com login Google."
      - "Tempo de build < 15 min (free tier)."
      - "Token Expo persistido em DEVICE_TOKENS apos primeiro login."
    environment: production
    requires_approval: true
    devops_security_sensitive: true
    handoff:
      do_not_reinvestigate: ["package-id-final"]
      required_test_first: null
      target_files: ["eas.json", "app.json"]
      minimal_change: "config build apenas"
      acceptance_checks:
        ["APK instala", "login funciona", "APK sem SERVICE_ROLE"]

  # ═══════════════════════ WAVE 6: FASE 6 SORTEIO + SUMULA ═══════════════════════
  - id: T6.1
    title: "Sorteio aleatorio puro + team_group/goalkeeper"
    description: "Implementar sorteio random puro conforme PRD regra 4: distribui os 14 jogadores confirmados (16 - 2 goleiros) aleatoriamente em 2 teams (team_group 1 e 2, 7 cada). Atribui is_goalkeeper=true aos 2 goleiro_pago. Function SQL `draw_teams(match_id)` que popula MATCH_PARTICIPANTS. Logica deve ser chamada pelo admin na UI quando comeca a partida."
    wave: 6
    agent: gem-implementer
    status: pending
    covers: [fase-6-sorteio]
    dependencies: [T2.3, T3.1, T7.2]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 4 sorteio aleatorio"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "Function draw_teams(match_id) popula MATCH_PARTICIPANTS."
      - "2 goleiros atribuidos is_goalkeeper=true (1 por team)."
      - "14 jogadores divididos 7/7 entre team_group 1 e 2."
      - "Sorteio e' aleatorio (execucoes diferentes geram times diferentes)."
      - "Chamada admin seta MATCHES.status='active' apos congelar lista."
    success_criteria:
      - "3 sorteios consecutivos geram combinacoes diferentes."
      - "Validacao: 2 goleiros em times diferentes."
    tech_stack: ["PostgreSQL functions", "Supabase RPC", "React Native"]
    test_coverage: "3 sorteios manuais no device"
    diag: null
    handoff:
      do_not_reinvestigate: ["sorteio-algoritmo"]
      required_test_first: null
      target_files:
        [
          "supabase/migrations/00000000000009_draw.sql",
          "app/(tabs)/sorteio.tsx",
        ]
      minimal_change: "function + UI chamada"
      acceptance_checks: ["sorteio OK 3x", "goleiros separados"]

  - id: T6.2
    title: "Sumula pos-jogo: placar + stats"
    description: "Tela de sumula pos-jogo (MATCH_PARTICIPANTS) que captura placar (MATCHES.team_scores jsonb) e estatisticas por jogador: goals_scored, goals_assisted, own_goals. UI admin para editar durante/pos jogo. Toggle 'finalizar partida' que seta MATCHES.status='finished'. Lista de MATCH_PARTICIPANTS carregada do sorteio T6.1, permitindo edicao."
    wave: 6
    agent: gem-implementer
    status: pending
    covers: [fase-6-sumula]
    dependencies: [T6.1]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 7 estatisticas + regra 4 sobrescrita"
    flags: { flaky: false, retries_used: 0, requires_design_validation: true }
    acceptance_criteria:
      - "UI carrega MATCH_PARTICIPANTS do match ativo."
      - "Edicao de placar em MATCHES.team_scores ({'1': X, '2': Y})."
      - "Edicao de goals_scored/assisted/own_goals por jogador."
      - "Botao 'Finalizar Partida' seta status=finished e dispara reminder goleiros."
      - "Add avulso nao-confirmado in-game (regra 4 do PRD)."
    success_criteria:
      - "Cenario completo: sorteio -> jogo -> sumula -> finalizar -> reminder."
    tech_stack: ["React Native", "Supabase JS", "NativeWind"]
    test_coverage: "teste manual cenario completo"
    diag: null
    handoff:
      do_not_reinvestigate: ["sumula-fields"]
      required_test_first: null
      target_files: ["app/(tabs)/sumula.tsx", "components/PlayerStatRow.tsx"]
      minimal_change: "UI sumula only"
      acceptance_checks:
        ["placar salvo", "stats salvas", "finalizar dispara reminder"]

  # ═══════════════════════ WAVE 7: FASE 8 PERFIL + REFINAMENTOS ═══════════════════════
  - id: T7.1
    title: "Tela Perfil + dados crus jogador"
    description: "Tela 'Perfil' mostrando: dados do PROFILES proprio, estatisticas crus agregadas (SUM goals_scored, assists, own_goals sobre MATCH_PARTICIPANTS historical). Sem calculos complexos (rendimento dupla etc fica pos-MVP). Links para logout, settings (notificacoes). UI PT-BR. Sem referencia ao campo morto 'avatar' (N2) - usar avatar_url se desejar exibir foto."
    wave: 7
    agent: gem-designer
    status: pending
    covers: [fase-8-perfil]
    dependencies: [T6.2]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 7 estatisticas"
    flags: { flaky: false, retries_used: 0, requires_design_validation: true }
    acceptance_criteria:
      - "Perfil mostra avatar_url (optional), nome, user_type badge."
      - "Estatisticas crus: total gols, assists, gols contra (soma simples)."
      - "Botao logout funcional."
      - "Sem calculos derivados/complexos (futuro pos-MVP)."
      - "Sem uso do campo morto 'avatar' (N2)."
    success_criteria:
      - "Usuario ve proprio historico de jogos."
    tech_stack: ["React Native", "NativeWind", "Supabase JS"]
    test_coverage: null
    diag: null
    handoff:
      do_not_reinvestigate: ["perfil-fields"]
      required_test_first: null
      target_files: ["app/(tabs)/perfil.tsx", "components/StatBadge.tsx"]
      minimal_change: "UI perfil only"
      acceptance_checks: ["perfil carrega", "somas corretas"]

  - id: T7.2
    title: "Onboarding + cadastro goleiro_pago admin UI"
    description: "Criar UI admin para cadastrar goleiro_pago SEM login (sem auth.users): admin entra full_name, atribui user_type='goleiro_pago'. Adicionalmente: tela de onboarding minimal para novo usuario OAuth (mostra PT-BR welcome, explica regras do racha). Complementar ao seed de goleiros feito em T1.3b (permite adicionar runtime)."
    wave: 7
    agent: gem-implementer
    status: pending
    covers: [fase-1-onboarding, fase-4-goleiro-cadastro]
    dependencies: [T1.7]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Regra 2 goleiros sem login"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    acceptance_criteria:
      - "UI admin cadastra goleiro_pago (full_name, sem email/auth)."
      - "Onboarding PT-BR exibido no primeiro login."
      - "Goleiro visivel no sorteio T6.1 (2 goleiros disponiveis: seed T1.3b + UI)."
    success_criteria:
      - "Admin cadastra goleiros via UI."
      - "Sorteio os usa como GK em separado."
    tech_stack: ["React Native", "Supabase JS"]
    test_coverage: "teste manual cadastro goleiro"
    diag: null
    handoff:
      do_not_reinvestigate: ["goleiro-cadastro-flow"]
      required_test_first: null
      target_files: ["app/(tabs)/admin/goleiros.tsx", "app/onboarding.tsx"]
      minimal_change: "UI only"
      acceptance_checks: ["goleiro criado", "sorteio usa"]

  # ═══════════════════════ WAVE 8: REFINAMENTO + REVIEW + TESTES ═══════════════════════
  - id: T8.1
    title: "Review final: RLS + paridade ERD + cron"
    description: "Review completo do backend: validar paridade ERD-PRD versus codigo SQL aplicado, RLS em 8 tabelas, 4 jobs cron ativos (dia 5 + Seg/Ter/Qui), trigger auth->PROFILES, function FIFO, draw_teams. Report todos divergencias."
    wave: 8
    agent: gem-reviewer
    status: pending
    covers: [fase-0-9-review]
    dependencies: [T1.7, T5.2, T5.0, T6.1]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Plano de verificacao PRD"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    requires_review: true
    review_depth: full
    review_security_sensitive: true
    acceptance_criteria:
      - "Report items: divergencias ERD, RLS, cron, triggers, vault secret."
      - "Coverage 100% das 8 tabelas e 4 jobs."
      - "Validar que APK (produto de T5.4) NAO expoe SERVICE_ROLE_KEY."
      - "Cross-check com plano de verificacao do PRD."
    success_criteria:
      - "Zero divergencias criticas, ou plano de mitigacao."
    handoff:
      do_not_reinvestigate: ["review-checklist"]
      required_test_first: null
      target_files: ["docs/plan/20260724-futamigos-mvp/review.md"]
      minimal_change: "documento de review"
      acceptance_checks: ["report entregue", "divergencias triadas"]

  - id: T8.2
    title: "E2E APK em device real (Maestro/Detox Android)"
    description: "Testes E2E manuais em APK Android real (saida do T5.4): todos fluxos criticos. Validar localizacao PT-BR, OAuth Google, push recebidos (token em DEVICE_TOKENS), WhatsApp share + fallback clipboard, sorteio, sumula. Cobertura da matriz de verificacao do PRD. Sem uso do campo morto 'avatar'."
    wave: 8
    agent: gem-mobile-tester
    status: pending
    covers: [fase-verificacao]
    dependencies: [T8.1, T5.4]
    conflicts_with: []
    context_files:
      - path: implementation_plan.md
        description: "Plano de verificacao PRD"
    flags: { flaky: false, retries_used: 0, requires_design_validation: false }
    validation_matrix:
      - scenario: "Login Google + token push persistido"
        steps:
          [
            "Abrir APK",
            "Tocar Entrar com Google",
            "Selecionar conta",
            "Aceitar permissoes notificacao",
          ]
        expected_result: "Session criada, PROFILES populado, DEVICE_TOKENS tem token, redirecionado para tabs"
      - scenario: "MATCHES auto-gestionado (T2.0)"
        steps:
          [
            "Apos db reset",
            "verificar proximo MATCHES quinta existe",
            "admin dispara job semanal manualmente",
          ]
        expected_result: "MATCHES da proxima quinta criado e visivel na tela presenca"
      - scenario: "RSVP mensalista"
        steps: ["Tela presenca", "Confirmar"]
        expected_result: "Usuario move para lista confirmados"
      - scenario: "Cutoff rebate FIFO"
        steps:
          [
            "Simular pos cutoff terca 19h BRT",
            "Mensalista desistente confirma",
            "validar INSERT waiting_list",
          ]
        expected_result: "Mensalista entra no final da fila FIFO como novo avulso"
      - scenario: "Promocao FIFO"
        steps: ["Mensalista desiste", "Admin promove 1o avulso"]
        expected_result: "Avulso promovido, proximo avulso assume fila"
      - scenario: "WhatsApp share + fallback clipboard"
        steps:
          [
            "Tela presenca",
            "Compartilhar",
            "Escolher WhatsApp (ou simular ausencia)",
          ]
        expected_result: "Texto formatado abre no WhatsApp nativo OU clipboard + toast se ausente"
      - scenario: "Sorteio"
        steps: ["Tela sorteio", "Sortear times"]
        expected_result: "14 jogadores divididos 7/7 + 2 goleiros separados"
      - scenario: "Sumula + finalizar"
        steps: ["Editar placar", "Add gol/assist", "Finalizar"]
        expected_result: "Stats salvas em MATCH_PARTICIPANTS, status=finished, push goleiros dispara"
      - scenario: "Push dia 5"
        steps: ["Disparar job cron manualmente"]
        expected_result: "Mensalidades geradas para todos mensalistas sem duplicar"
    success_criteria:
      - "Todos cenarios da matriz passam em device real."
      - "Zero crashes em fluxo happy path."
      - "Token Expo visivel em DEVICE_TOKENS."
    cleanup: false
    test_data:
      ["2 contas Google teste", "mensalistas seed", ">=2 goleiro_pago seed"]
    handoff:
      do_not_reinvestigate: ["device-android-teste"]
      required_test_first: null
      target_files: ["docs/plan/20260724-futamigos-mvp/e2e-report.md"]
      minimal_change: "report apenas"
      acceptance_checks:
        ["matriz 100% pass", "report entregue", "sem campo morto avatar"]
